# IMPORTANTE: Em producao, gere uma chave secreta com:
#   python -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())"
SECRET_KEY=change-me-to-a-random-key

# Em producao, altere para False
DEBUG=True

# Em producao, liste apenas os dominios permitidos (ex: meusite.com,api.meusite.com)
ALLOWED_HOSTS=*

DB_NAME=kanban_db
DB_USER=kanban_user
DB_PASSWORD=kanban_pass
DB_HOST=db
DB_PORT=5432

CORS_ALLOWED_ORIGINS=http://localhost:3000,http://localhost:3022
CSRF_TRUSTED_ORIGINS=http://localhost:3000,http://localhost:3022

DJANGO_ADMIN_USER=admin
DJANGO_ADMIN_EMAIL=admin@example.com
# IMPORTANTE: Em producao, use uma senha forte
DJANGO_ADMIN_PASSWORD=admin
